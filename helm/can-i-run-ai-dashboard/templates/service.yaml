apiVersion: v1
kind: Service
metadata:
  name: {{ include "can-i-run-ai-dashboard.fullname" . }}
  labels:
    {{- include "can-i-run-ai-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "can-i-run-ai-dashboard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
